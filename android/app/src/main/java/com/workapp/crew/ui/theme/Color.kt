package com.workapp.crew.ui.theme

import androidx.compose.ui.graphics.Color

// Soft Focus — the palette picked from the web Style Lab mockups (see web/tailwind.config.js,
// which carries the same values under the "brand" and "slate" scales). Kept in one place so the
// web and Android clients read as the same product.

val SoftFocusPrimary = Color(0xFF8B6F9E)
val SoftFocusPrimaryDeep = Color(0xFF6E5580)
val SoftFocusPrimaryContainer = Color(0xFFF1ECF7)
val SoftFocusOnPrimaryContainer = Color(0xFF6E5580)

val SoftFocusBackground = Color(0xFFFAF9FC)
val SoftFocusSurface = Color(0xFFFFFFFF)
val SoftFocusSurfaceVariant = Color(0xFFF3F1F8)
val SoftFocusOutline = Color(0xFFE9E5F1)

val SoftFocusInk = Color(0xFF221F2B)
val SoftFocusInkMuted = Color(0xFF837C93)
